# Active Spring profile for local backend execution
SPRING_PROFILES_ACTIVE=dev-local

# Backend
SERVER_PORT=8080
FRONTEND_URL=http://localhost:3000

# MariaDB
DB_NAME=association
DB_USER=association_user
DB_PASSWORD=association_password
DB_ROOT_PASSWORD=root_password

# Use 3308 on the host to avoid conflicts with local MariaDB installations.
DB_HOST=127.0.0.1
DB_PORT=3308
DB_HOST_PORT=3308
DB_CONTAINER_PORT=3306

# Security
JWT_SECRET=dev_secret_key_for_tfg_dam_project_2026_change_me_please
JWT_EXPIRATION_MS=86400000

# Frontend
NEXT_PUBLIC_API_URL=http://localhost:8080/api/v1
